/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'cdn-icons-png.flaticon.com', 'cdn-icons-png.flaticon.com', 'img.icons8.com'],
    },
};

export default nextConfig;
